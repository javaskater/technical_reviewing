```scala
scala> class Person(val name:String){
     | override def toString = s"{getClass.getName}[name=$name]"
     | }
class Person

scala> class secretAgent(codeName:String) extends Person(codeName){
     | override val name = "secret"
     | override def toString = "secret"
     | def show():Unit = {
     | println(super.toString)
     | println("The actual name is:"+ name)
     | println("the actual toString" + toString)
     | println("the actual class is:" + getClass.getName)
     | }
     | }
class secretAgent

scala> val s = new secretAgent("MENA")
val s: secretAgent = secret

scala> s.toString
val res0: String = secret

scala> s.show()
{getClass.getName}[name=secret]
The actual name is:secret
the actual toStringsecret
the actual class is:$line5.$read$$iw$secretAgent
```
* name has been overriden, so contrary to the previous chapter is is no a new variable
# 96
* a val can only overrides another val o a parameterless def!!!
* In the table 8.2:
  * The columns reprensent the overidden attribut
  * The lines represent what they can override (the super-class attirbutes)