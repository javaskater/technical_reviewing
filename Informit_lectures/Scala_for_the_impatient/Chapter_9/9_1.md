* An exception is thrown if the path is not correct (it does not accept ~)
```scala
scala> import scala.io.Source
import scala.io.Source

scala> val source = Source.fromFile("~/ERICA/TOPAD/TOPAD-Cible/STRUCTURES20240301", "UTF-8")
java.io.FileNotFoundException: ~/ERICA/TOPAD/TOPAD-Cible/STRUCTURES20240301 (Aucun fichier ou dossier de ce type)
  at java.base/java.io.FileInputStream.open0(Native Method)
  at java.base/java.io.FileInputStream.open(FileInputStream.java:216)
  ... 37 elided

scala> val source = Source.fromFile("/home/jmena01/ERICA/TOPAD/TOPAD-Cible/STRUCTURES20240301", "UTF-8")
val source: scala.io.BufferedSource = <iterator>
scala> val iterator = source.getLines()
val iterator: Iterator[String] = <iterator> //it gives us the exact type !!!
scala> for (l <- iterator){
     | println (l)
     | } //It does print me the entire file
```
* After it has been used the ierator is empty, you have to:
  * reload the BufferedSource with the File
  * re-create the iterator to use it
## The to Array method
```scala
scala> val source = Source.fromFile("/home/jmena01/ERICA/TOPAD/TOPAD-Cible/STRUCTURES20240301", "UTF-8")
val source: scala.io.BufferedSource = <iterator>

scala> val lines = source.getLines().toArray
val lines: Array[String] = Array("          STRUCTURES 202403010000057927       ...") // the REPL prints the first line

scala> println(s"int the file there is ${lines.size} lines")
int the file there is 57928 lines
//If I recall it without reloading the Source File
scala> val lines = source.getLines().toArray
val lines: Array[String] = Array()

scala> println(s"int the file there is ${lines.size} lines")
int the file there is 0 lines
```
## mkString keeps the formatting
* it returns a String
* it empties the Buffer!
# Handling char by char
```scala
scala> val source = Source.fromFile("/home/jmena01/ERICA/TOPAD/TOPAD-Cible/recettesRFFevrier.csv", "UTF-8")
val source: scala.io.BufferedSource = <iterator> //it is a BufferedSource of String 

scala> 

scala> val iter = source.buffered
val iter: scala.collection.BufferedIterator[Char] = <iterator> //From String we are down to char
```