```scala
scala> val source1 = Source.fromURL("https://example.intranet/", "UTF-8")
val source1: scala.io.BufferedSource = <iterator> //We obtains the same object as 9_1
scala> val lines = source1.getLines()
val lines: Iterator[String] = <iterator>
scala> for (l <- lines){
     | println(s"ligne |${l}|")
     | }
```
* We do print line by line the main page otf the intranet
  * for the internet you have to introduce a proxy configuration
* The __Source.fromString__ does not give an Buffer but a character iterator !!! 