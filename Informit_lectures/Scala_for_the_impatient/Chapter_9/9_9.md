* In the REPL
```scala
scala> "ls -al ..".! //It is a String it does not accept a ! method !!!
                   ^
       error: value ! is not a member of String

scala> import scala.sys.process._
import scala.sys.process._

scala> "ls -al ..".! //It is no more a String but a process
total 20
drwxr-xr-x  5 jmena01 domain users 4096 mars  21 14:49 .
drwx------ 47 jmena01 domain users 4096 mars  26 08:38 ..
drwxr-xr-x  7 jmena01 domain users 4096 févr.  2 16:42 erica-datatransform-rescrits
drwxr-xr-x  9 jmena01 domain users 4096 mars  21 17:46 net.jgp.books.spark.ch01
drwxr-xr-x  7 jmena01 domain users 4096 mars  26 10:04 testScala
val res1: Int = 0 // the shell returned 0: everything OK
scala> "ls -al hello".!
val res3: Int = 2 //The shell returned 2 because hello file or dir dos not exist in the current dir
ls: impossible d'accéder à 'hello': Aucun fichier ou dossier de ce type
```
> The scala.sys.process package contains an [implicit conversion](https://docs.scala-lang.org/tour/implicit-conversions.html) from strings to
> ProcessBuilder objects. The ! method executes the ProcessBuilder object.
## The commands
* The syntax of the commands depends on the underlying operating system
```scala
scala> import java.io.File
import java.io.File

scala> ("ls -al /" #> new File("/tmp/ls.log")).!
val res3: Int = 0
scala> ("grep u" #< new File("/tmp/ls.log")).! //reimporting the file
```
* intere    sting official resource about the [scala.sys.process library](https://www.scala-lang.org/api/2.13.3/scala/sys/process/index.html)
# using he Process object
```scala
package com.absile.scala.chap09
import scala.sys.process._
import java.io._

object Test9_9 extends App{
    val p = Process("tee toto.log", new File("/home/jmena01/ERICA/TOPAD/TOPAD-Cible"), ("LANG", "UTF-8"))
    val ret = ("echo 42" #| p).!
    println(f"[main] the command has a return of $ret")
}
```
* If I go to _toto.log_ in _/home/jmena01/ERICA/TOPAD/TOPAD-Cible_
  * I find the 42
```bash
jmena01@M077-1840900:~/ERICA/TOPAD/TOPAD-Cible$ cat toto.log 
42
```