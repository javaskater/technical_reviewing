# p 112 / 167

* when the flex direction is not specified it is by default _row_
  * the the tiles have the same height
  * except that for the _column-sidebar_ there is no flex specified
    * the 2 div stack over each other without filling the remaining space

# p 114/167
* I don't find where the mrgin-top of he button comes from ?
  * even when adding navigator styles
  * The inputs and the buttons must have default margins but they are not shown here
* _cursor: pointer_ is for a hand with a **pointing** finger
